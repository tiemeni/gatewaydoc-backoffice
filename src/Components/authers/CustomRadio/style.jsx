import { createStyles } from "@mui/material";

const styles = createStyles({
  inputContainer: {
    display: "flex",
    alignItems: "center",
    gap: 2,
  },
  label: {
    width: 250,
    textAlign: "end",
    fontSize: 12,
    fontWeight: 600,
  },
});

export default styles;
