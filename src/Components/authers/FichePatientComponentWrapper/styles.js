import { makeStyles } from '@mui/styles';

const styles = makeStyles({
    form: {
        display: 'flex',
        maxHeight: '65vh',
        overflow: 'scroll',
        marginBottom: '2px'
    },
    search:{
        borderRadius: "30px",
        position: "relative",
        top: "35%"
    },
    stepper: {
        width: '100%',
        marginTop: '15px'
    }
})
export default styles;